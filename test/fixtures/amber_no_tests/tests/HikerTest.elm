module HikerTest exposing (double)

{-| The learner has deleted the test and left the helper behind. The module is
still gathered, because it sits in tests/, but it exposes no value of type
Test, so there is nothing to run.
-}


double : Int -> Int
double n =
    2 * n
